# Diff 方案分析

## 背景

这份文档记录的是在实现版本对比能力之前，对 `huashuiai-web` 真实项目所做的核心分析结论。

它回答的不是“当前 demo 代码怎么组织”，而是：

- 在现有 `Tiptap + ProseMirror + Yjs` 编辑器前提下，版本 diff 应该如何定义问题
- 为什么要把版本恢复和版本预览区分开
- 为什么当前更适合采用块级预览 diff，而不是直接走更重的富文本精确变更追踪

具体的 diff 结构、归一化、匹配和渲染实现，见：

- [docs/current-diff-design.md](/Users/admin/Documents/temp/tiptap-diff-demo/docs/current-diff-design.md:1)

## 目标问题

版本 diff 要解决的核心问题是：

- 用户选中某个历史版本后，能快速理解“它和当前版本相比到底改了什么”
- 差异结果要尽量保留文档结构，而不是退化成难以阅读的原始文本或 HTML 噪音
- 版本预览要服务于“查看”和“恢复”决策，而不是追求审计级别的底层操作回放

所以，这里的 diff 目标更接近：

- 面向阅读的版本预览

而不是：

- 面向底层协同状态的二进制对比
- 面向编辑步骤的精确操作回放
- 面向富文本 AST 的完全等价最优 diff

## 现有源码前提

结合 `huashuiai-web` 当前源码，可以确认几个重要前提：

1. 正文编辑不是传统表单保存模式，而是协同编辑模式。
2. 编辑器底座是 `Tiptap + ProseMirror + Yjs`。
3. 当前正文既有“人可读”的内容表示需求，也有“协同可恢复”的内容表示需求。

这意味着版本体系里至少有两类数据职责：

- 可读内容
  - 用于查看、预览、对比、调试
- 协同状态内容
  - 用于恢复协同编辑状态

如果把这两种职责混在一起，版本 diff 和版本恢复都会变得更复杂。

## 为什么要区分 `content` 和 `content_binary`

这是整个方案里最重要的判断之一。

### `content`

`content` 更适合作为：

- diff 输入
- 预览输入
- 调试输入

原因是它天然更接近文档语义层，可以被归一化为标题、段落、列表、表格、图片、分栏等结构。

### `content_binary`

`content_binary` 更适合作为：

- 恢复输入
- 协同状态回放输入

原因是它保留的是协同编辑所需的状态信息，而不是直接面向用户阅读的结构化内容。

### 核心结论

因此版本能力应拆成两条路径：

- 版本 diff
  - 基于 `content`
- 版本恢复
  - 基于 `content_binary`

不能指望同一种底层表示同时优雅地服务这两件事。

## 为什么首版不该过度设计

在版本 diff 这件事上，最容易走偏的方向，是一开始就把目标设成：

- 完整富文本 AST 精准 diff
- Yjs 底层状态精确对比
- 所有块类型一次性支持到位
- 与协同编辑历史完全一致的操作级变更追踪

这些方向并不是做不到，而是它们解决的是“更重的问题”，而不是首版版本预览真正最优先的问题。

首版更重要的是：

- 差异是否可读
- 结构是否尽量保留
- 复杂块能否逐步扩展
- 和现有编辑器的接入成本是否可控

这也是为什么当前设计最终收敛为“块级版本预览 diff”。

## diff 方案候选与取舍

在分析阶段，主要有几类可选方案。

### 1. 直接做纯文本 diff

优点：

- 实现简单
- 稳定
- 接入成本低

问题：

- 标题、表格、任务、图片、分栏等结构都会被压平
- 最终效果更像代码 diff，而不是文档版本预览

纯文本 diff 适合作为验证方向，但不适合作为最终方案的上限。

### 2. 基于 HTML 做 diff

优点：

- 数据容易拿到

问题：

- HTML 标签噪音很重
- 很难保留稳定的文档语义
- 最终预览容易受渲染细节影响

这条路不适合当前项目。

### 3. 基于 Tiptap JSON 做结构化 diff

优点：

- 能保留文档结构
- 可按块类型逐步增强
- 更贴近 Notion 风格的版本预览

问题：

- 需要自己定义归一化、匹配、分类和渲染层

当前设计最终选择了这条路，但不是做“全量 AST 精准 diff”，而是做“块级预览 diff”。

### 4. 直接接入 Tiptap 官方高级 diff 能力

这条路在当前项目里不适合作为首选。

原因主要有两个：

1. 当前项目的 Tiptap 依赖还是 `2.x` 体系。
2. 官方 `AI Toolkit / Suggestion Diff View` 属于另一套能力体系，接入成本和版本约束都更高。

因此，现阶段不应把官方高级 diff 工具当作低成本方案。

## 当前推荐方向：块级版本预览 diff

基于前面的取舍，当前推荐方向是：

- 以 Tiptap JSON 为输入
- 先归一化为统一块模型
- 先比较块，再在块内比较文本或局部结构
- 复杂块优先保留结构和语义

也就是：

- 先判断“哪一块变了”
- 再判断“块里面哪里变了”

这条路线的核心优势是：

- 比纯文本更接近真实文档阅读体验
- 比完整 AST 精确 diff 更容易落地
- 能逐步支持更多复杂块，而不是一开始把问题做死

## 当前 diff 设计的核心语义

根据 [docs/current-diff-design.md](/Users/admin/Documents/temp/tiptap-diff-demo/docs/current-diff-design.md:1)，当前方案的核心语义是：

### 1. 比较单位是“块”

不是把整篇文档压成一大段字符串去比较，而是先拆成：

- `heading`
- `paragraph`
- `listItem`
- `taskItem`
- `blockquote`
- `codeBlock`
- `horizontalRule`
- `imageBlock`
- `table`
- `columnGroup`

等不同块类型。

### 2. 块级 diff 和块内 diff 分开

块级 diff 负责判断：

- `added`
- `removed`
- `modified`
- `unchanged`

块内 diff 只在需要时发生：

- 文本类块做字符级 diff
- 任务项同时比较文本和 `checked`
- 表格递归到 cell
- 图片和分割线按结构语义处理

### 3. 复杂块优先保留结构

这点很关键。

当前设计不是把复杂块强行转成纯文本，而是尽量保留：

- 表格结构
- 分栏结构
- 图片替换语义
- 分割线结构语义

这决定了最终的 diff 结果会更像“文档预览”，而不是“扁平文本改动记录”。

## 为什么这个方向更适合真实项目

从真实项目角度看，这套块级预览 diff 有几个明显优势。

### 1. 它更符合用户理解文档的方式

用户不是按 AST 节点理解文档，也不是按 Yjs 状态理解文档。

用户是按：

- 标题
- 段落
- 列表
- 表格
- 图片
- 分栏

理解文档的。

块级 diff 更贴近这种认知模型。

### 2. 它天然支持逐步扩展

当前先支持一批常见块类型，后续再增强：

- 代码块行级 diff
- 表格更细粒度匹配
- 行内 marks 差异
- 更强的块匹配算法

不需要一开始就把所有问题一次性解决。

### 3. 它把复杂度压在可控边界内

当前链路明确拆成：

- `normalize`
- `engine`
- `renderer`

这样复杂度不会直接泄漏到页面层或业务层。

## 当前方案的边界

即使当前方向合理，也要明确它不是“最终极 diff”。

结合现有设计，主要边界包括：

1. 匹配策略仍然是轻量的顺序优先 + 小范围 lookahead，不是最优匹配。
2. 文本类块当前主要是字符级 diff，长文本变化时可能偏碎。
3. 分栏、表格等复杂结构当前有明确约束，不处理所有重排场景。
4. 行内 marks 差异还没有单独展开。

这些边界是当前方案有意接受的成本，用来换取：

- 可读性
- 接入可控
- 结构保留
- 逐步增强能力

## 结论

当前阶段，版本 diff 的合理方向不是：

- 做 Yjs 二进制 diff
- 做完整富文本 AST 精确 diff
- 直接依赖 Tiptap 官方高级 diff 工具

而是：

- 用 `content` 承担版本预览输入
- 用 `content_binary` 承担版本恢复输入
- 用块级预览 diff 承担“版本差异可读化”的核心职责

一句话总结：

**当前最适合 `huashuiai-web` 的版本 diff 方向，是基于 Tiptap JSON 的块级版本预览 diff：先比较块，再在块内比较内容；复杂块优先保留结构与语义，而不是全部压平成纯文本。**
